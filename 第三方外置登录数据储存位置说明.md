# 第三方外置登录数据储存位置说明

## 概述

Plain Craft Launcher 2 (PCL) 支持多种第三方外置登录方式，登录数据通过加密方式存储在 Windows 注册表中。

## 存储位置

### 1. Windows 注册表路径
所有加密的登录数据都存储在：
```
HKEY_CURRENT_USER\Software\PCLDebug\
```

### 2. 存储的登录信息

#### A. 统一通行证 (Nide) 登录数据
- **LoginNideEmail** - 统一通行证账号邮箱（已加密）
- **LoginNidePass** - 统一通行证密码（已加密）
- **CacheNideAccess** - 访问令牌（已加密）
- **CacheNideClient** - 客户端令牌（已加密）
- **CacheNideUuid** - 用户UUID（已加密）
- **CacheNideName** - 用户名称（已加密）
- **CacheNideUsername** - 用户账号（已加密）
- **CacheNidePass** - 缓存的密码（已加密）
- **CacheNideServer** - 服务器地址（已加密）

#### B. Authlib-Injector 外置登录数据
- **LoginAuthEmail** - 外置账号邮箱（已加密）
- **LoginAuthPass** - 外置密码（已加密）
- **CacheAuthAccess** - 访问令牌（已加密）
- **CacheAuthClient** - 客户端令牌（已加密）
- **CacheAuthUuid** - 用户UUID（已加密）
- **CacheAuthName** - 用户名称（已加密）
- **CacheAuthUsername** - 用户账号（已加密）
- **CacheAuthPass** - 缓存的密码（已加密）
- **CacheAuthServerServer** - 服务器地址（已加密）
- **CacheAuthServerName** - 服务器名称（已加密）
- **CacheAuthServerRegister** - 注册地址（已加密）

#### C. 其他登录相关设置
- **LoginRemember** - 是否记住密码（已加密）
- **LoginType** - 登录类型
- **LoginPageType** - 登录页面类型

## 实现细节

### 代码位置

1. **设置定义**
   - 文件：`Plain Craft Launcher 2/Pages/PageSetup/ModSetup.vb`
   - 行：59-83
   - 所有登录相关的设置项都在 `SetupDict` 字典中定义，标记为 `Source:=SetupSource.Registry` 和 `Encoded:=True`

2. **注册表读写函数**
   - 文件：`Plain Craft Launcher 2/Modules/Base/ModBase.vb`
   - 函数：
     - `ReadReg(Key, DefaultValue)` - 读取注册表（行 590）
     - `WriteReg(Key, Value)` - 写入注册表（行 601）
     - `HasReg(Key)` - 检查键是否存在（行 614）
     - `DeleteReg(Key)` - 删除注册表键（行 620）

3. **登录页面实现**
   - 统一通行证登录：`Plain Craft Launcher 2/Pages/PageLaunch/PageLoginNide.xaml.vb`
   - Authlib-Injector 登录：`Plain Craft Launcher 2/Pages/PageLaunch/PageLoginAuth.xaml.vb`

### 加密方式

- 所有标记为 `Encoded:=True` 的设置项都会使用 `SecretEncrypt` 函数进行加密
- 加密密钥：`"PCL" & Identify`（其中 Identify 是用户特定的标识符）
- 解密时使用 `SecretDecrypt` 函数

### 数据格式

- **多账号支持**：邮箱和密码使用 `¨` 符号分隔多个账号
  - 例如：`账号1¨账号2¨账号3`
- **读取时**：使用 `Split("¨")` 分割字符串
- **选择账号**：通过索引访问对应的账号和密码

## 安全性说明

1. 所有敏感数据（邮箱、密码、令牌）都经过加密存储
2. 加密密钥与用户标识符绑定，提供额外的安全性
3. 数据存储在用户级别的注册表中，其他用户无法访问

## 相关文件

| 文件路径 | 说明 |
|---------|------|
| `Plain Craft Launcher 2/Pages/PageSetup/ModSetup.vb` | 设置项定义和管理 |
| `Plain Craft Launcher 2/Modules/Base/ModBase.vb` | 注册表读写基础函数 |
| `Plain Craft Launcher 2/Pages/PageLaunch/PageLoginNide.xaml.vb` | 统一通行证登录界面 |
| `Plain Craft Launcher 2/Pages/PageLaunch/PageLoginAuth.xaml.vb` | Authlib-Injector 登录界面 |
| `Plain Craft Launcher 2/Modules/Minecraft/ModMinecraft.vb` | Minecraft 相关功能，包含服务器验证逻辑 |
| `Plain Craft Launcher 2/Modules/Minecraft/ModLaunch.vb` | 启动相关逻辑，包含登录验证 |

## 数据流程

1. **用户登录**：
   - 用户在登录页面输入账号密码
   - 调用 `Setup.Set()` 保存到注册表（自动加密）
   
2. **读取登录信息**：
   - 调用 `Setup.Get()` 从注册表读取（自动解密）
   - 填充到登录界面的控件中

3. **验证流程**：
   - 检查 `CacheNideAccess` 或 `CacheAuthAccess` 是否为空
   - 如果为空，需要重新登录
   - 验证成功后，更新访问令牌和用户信息

## 查看和管理数据的方法

### 方法1：使用注册表编辑器
1. 按 `Win + R` 打开运行对话框
2. 输入 `regedit` 并回车
3. 导航到 `HKEY_CURRENT_USER\Software\PCLDebug\`
4. 可以查看所有存储的键值（注意：数据是加密的）

### 方法2：通过 PCL 启动器
- PCL 提供了相应的登录界面来管理这些账号
- 可以通过"记住密码"功能控制是否保存密码

### 方法3：编程方式
```vb
' 读取统一通行证邮箱
Dim email = Setup.Get("LoginNideEmail")

' 读取 Authlib 邮箱
Dim authEmail = Setup.Get("LoginAuthEmail")

' 清除缓存的访问令牌（强制重新登录）
Setup.Set("CacheNideAccess", "")
Setup.Set("CacheAuthAccess", "")
```

## 注意事项

1. **数据迁移**：如果更换计算机，需要重新登录，因为加密密钥与用户环境相关
2. **安全备份**：虽然数据已加密，但仍建议定期更换密码
3. **调试模式**：注册表路径包含"PCLDebug"，这可能是开发时的命名
4. **密码管理**：如果不需要记住密码，可以关闭"记住密码"功能

## 总结

PCL 的第三方外置登录数据主要存储在 Windows 注册表的 `HKEY_CURRENT_USER\Software\PCLDebug\` 路径下，所有敏感信息都经过加密处理。支持两种主要的第三方登录方式：
1. **统一通行证 (Nide8)** - 使用 `LoginNide*` 和 `CacheNide*` 键
2. **Authlib-Injector** - 使用 `LoginAuth*` 和 `CacheAuth*` 键

用户数据的安全性通过加密和用户级别的注册表访问控制来保障。
